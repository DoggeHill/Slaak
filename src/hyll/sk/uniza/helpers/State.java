package hyll.sk.uniza.helpers;

/**
 * Enum used in previous(historical) versions of this app
 * Probably will be deleted in our future release
 * TODO: safe delete
 */
public enum State {
    BUFFERED,
    SENDER,
    RECEIVER
}
